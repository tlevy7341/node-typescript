//import request from "supertest";
//import app from "../app";

/* describe("GET /", () => {
  describe("Make sure get is valid", () => {
    it("should return 200", async () => {
      const response = await request(app).get("/");
      expect(response.body).toEqual({ msg: "Hello World!" });
      expect(response.status).toBe(200);
    });
  });
}); */
